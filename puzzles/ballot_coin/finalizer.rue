// ballot_coin/finalizer.rue
// Finalizer for the Ballot Coin.
//
// Standard CHIP-0050 finalizer: recreates the Ballot Coin singleton
// at amount 1 after each inner action runs. The Ballot Coin never
// accumulates fees, so the recreated amount is always 1.
//
// Recreation puzzle hash format:
//   curry_tree_hash(
//     ACTION_LAYER_MOD_HASH,
//     [
//       FINALIZER_FULL_HASH,
//       BALLOT_ACTIONS_MERKLE_ROOT,
//       new_state,
//     ]
//   )

import super::action::StateTruth;
import super::finalizer::*;
import shared::*;

fn main(
    ACTION_LAYER_MOD_HASH: Bytes32,
    HINT: Bytes32,
    // 2nd curry
    FINALIZER_SELF_HASH: Bytes32,
    Truth: FinalizerTruths,
    last_action_output: (StateTruth, List<List<Condition>>),
    ..._my_solution: Any
) -> List<Condition> {
    let new_state = unchecked_cast::<BallotState>(last_action_output.first.actual_state);

    let recreation_condition = CreateCoin {
        puzzle_hash: curry_tree_hash(
            ACTION_LAYER_MOD_HASH,
            [
                curry_tree_hash(
                    FINALIZER_SELF_HASH,
                    [
                        tree_hash_atom(FINALIZER_SELF_HASH),
                    ]
                ),
                tree_hash_atom(Truth.Merkle_Root),
                tree_hash(new_state),
            ]
        ),
        amount: 1,
        memos: Memos {
            value: [HINT],
        }
    };

    [
        recreation_condition,
        ...flatten_list(last_action_output.rest)
    ]
}
